/**
 * Notification Types Enum
 * Defines all possible notification types in the application
 */
export enum NotificationType {
  LIKE = "like",
  COMMENT = "comment",
  FOLLOW = "follow",
  MENTION = "mention",
  MESSAGE = "message",
}
